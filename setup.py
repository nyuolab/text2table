from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Convert texts into tables with the power of magic.',
    author='OLab',
    license='MIT',
)
